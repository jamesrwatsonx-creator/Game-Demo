# Constraint Ledger

The current dependency-aware constraint state for the game.

| ID | Claim | Type | Evidence | Confidence | Dependencies | Falsifier | Status | Affected branches |
|---|---|---|---|---:|---|---|---|---|
| CL-00 | Web/TS+Three.js stack materially beats Unity/C# for *this container's* iteration/determinism/perf-observability/testing needs. | Derivable (given environment facts) | No Unity/Xcode/Android SDK installed in container (observed); Node+Playwright/Chromium present (observed). | High (env-scoped) | D-001 | Unity tooling becomes available in-session AND a web build measurably fails mobile perf a Unity build would meet. | supported | all |
| CL-01 | Three lanes gives the strongest baseline balance of speed/readability/swipe precision in portrait. | Inferable | Web research (this session) confirms 3-lane swipe left/right/up/down is Subway Surfers' actual, well-documented design, and is described by secondary sources as making the game "not stressful even at high speeds because the lane structure makes it easy to anticipate obstacles" — corroborates the convention exists and is credited with readability, but is not a controlled study proving 3 is optimal vs. 4. | Medium (upgraded from genre-convention-only) | camera choice, lane width math | A 4-lane prototype shows equal/better comprehension+precision+tension without more accidental overshoot. | open — building at 3 lanes as default, keeping lane count as a config constant so 4-lane is a cheap follow-up test | render, input |
| CL-02 | Gesture recognition must produce immediate logical response and visibly begin movement inside the perceptual latency budget. | Derivable → Observed (cited) | Web research (this session): ~100ms is the long-cited "feels instantaneous" threshold; more granular HCI findings cite tap-latency imperceptibility below ~24ms and a just-noticeable-difference around ~64ms for touch events (sources below) — the 100ms figure is a coarse, widely-used engineering rule of thumb, not a single precise constant. This session's own instrumentation (Playwright) measured ~1.1-1.3ms from gesture-detected to next-rendered-frame for the JS-side chain only (excludes real touch-hardware latency, not observable in this container). | Medium-High | input layer, render interpolation | Slower response shows equal perceived control with no increase in accidental deaths. | supported (JS-side chain only) — comfortably under every cited threshold; real end-to-end device latency unmeasured, see GAPS.md | input |
| CL-03 | A literal shared head-on camera may become unreadable as closing speed increases. | Derivable → Observed | Combined-closing-speed reasoning was the *original* hypothesis; the actual observed failure mode at matched 94km/h/~1450m (test-results/camera-headon-highspeed.png vs camera-mirrored-highspeed.png, WL-06/07/08) was cross-player obstacle overlap from sharing one lane corridor, not foreshortening from closing speed per se. | High (conclusion) / Medium (original mechanism) | camera/perspective choice (CL-07), D-004 | An ordinary perspective camera preserves readability across the full speed curve without special treatment. | **supported — but by a different mechanism than hypothesized**; see D-004 | render |
| CL-04 | Opponent-created hazards need stronger telegraphing than ordinary course obstacles because they carry perceived-fairness risk. | Inferable | Competitive-fairness literature/genre convention; not directly measured against humans. | Medium | effects system, telegraph timing | Testers reliably counter equally short telegraphs with no increase in unfair-death reports. | supported (mechanically) — `injectOpponentHazard` enforces a 1.6x lead-time multiplier over the ordinary reaction floor, unit-tested (tests/unit/reaction-window.test.ts); *perceptual* fairness untested — see G-01 | sim/effects |
| CL-05 | Deterministic seed-based simulation reduces multiplayer sync complexity. | Derivable | Standard lockstep/deterministic-replay argument; same input sequence + same seed ⇒ same state trace. | High | sim architecture | Identical inputs fail to reproduce identical states across platforms/network conditions. | supported — sim is pure functions over fixed-step state with no `Math.random`/`Date.now`/wall-clock dependence anywhere (verified by code inspection + reproducible test runs across 10,000+ seeds, OBS-01/OBS-02); cross-platform (i.e. actual device) reproducibility remains untested — see GAPS.md | sim |
| CL-06 | Spectating the surviving opponent after death increases rivalry/rematch motivation. | Inferable | Genre convention (spectator/eliminated-player patterns in PvP mobile games); no human testers available in this session to confirm. | Low (untested here) | UI/flow | Users consistently skip it or report lower rematch intent. | open — implemented as a fast (~1.4s), tap-to-skip "OPPONENT DOWN — NEXT CHALLENGER" banner, deliberately short given untested status; not validated with humans | ui, gaps |
| CL-07 | Mirrored linked worlds communicate competitive interaction more clearly than literal physical head-on collision courses. | Open hypothesis → Observed | Direct screenshot comparison at matched difficulty tiers (see CL-03). | Medium-High | render/camera, CL-03, D-004 | Direct comparison of both modes (built in this prototype) shows head-on is equally or more readable at top difficulty tier. | **supported** for this implementation — mirrored stayed independently legible at every tier tested; head-on degraded at the top tier. Shipped as default (D-004) | render |

## Status vocabulary

`proposed` · `active` · `supported` · `weakened` · `falsified` · `superseded` · `unresolved`

When a claim breaks, invalidate only downstream branches that actually depend on it. Record the invalidation rather than restarting unrelated work.